# comment
drive --devpath=/dev/sda --ssd
    partition --size 100M --bootable --label BOOTPART --code MyXXXXXCODE
        encryption
            filesystem --fstype fat --mountpoint /boot
    partition --size max --label "SYS PART"
            filesystem --fstype btrfs --mountpoint /

